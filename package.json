{
  "name": "reader",
  "private": true,
  "version": "0.1.48",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "clean": "npm run clean:knip",
    "clean:knip": "knip --fix",
    "clean:src-css": "node clean-css.js --src",
    "clean:css": "npm run build && node clean-css.js",
    "clean:dist": "node --eval \"import('node:fs').then(m=>m.rmSync('dist',{recursive:true,force:true}))\"",
    "preview": "vite preview",
    "prepare": "git config core.hooksPath .githooks"
  },
  "dependencies": {
    "dexie": "^4.0.9",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-icons": "^5.6.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "knip": "^6.4.1",
    "purgecss": "^8.0.0",
    "vite": "^5.4.8",
    "vite-plugin-pwa": "^0.20.5"
  }
}
